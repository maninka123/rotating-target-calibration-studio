# Documentation assets

The main README uses `simulator-demo.gif` (10 seconds) and `rotation-view.png` from `npm run capture:demo`, plus `estimation-overlay.png` and `sweep-results.png` from the Playwright application test. To capture an already running build, set `CAPTURE_URL` to its local URL.
